

-module(mongo_config).

-export([ 
         get_drite/0,
         get_host/0,
         get_port/0,
         get_pool_num/0,
         get_process_num/0,
         get_db/0
        ]).

-include("mongo.hrl").

% ----------------------------------------------------
% @doc 测试的 drive 类型
% ----------------------------------------------------
get_drite() ->
    ?MO_TYPE_MONGODB_1.

% ----------------------------------------------------
% @doc 用于测试的主机
% ----------------------------------------------------
get_host() ->
    "localhost".

% ---------------------------------------------------
% @doc 测试的端口号
% ---------------------------------------------------
get_port() ->
    27017.

% ---------------------------------------------------
% @doc 测试的数据库
% ---------------------------------------------------
get_db() ->
    <<"drive_test">>.

% ---------------------------------------------------
% @doc 测试的连接缓冲池数量
% ---------------------------------------------------
get_pool_num() ->
    32.

% ---------------------------------------------------
% @doc 测试的进程数量
% ---------------------------------------------------
get_process_num() ->
    32.



